import { ScrollNavDirective } from './scroll-nav.directive';

describe('ScrollNavDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollNavDirective();
    expect(directive).toBeTruthy();
  });
});
