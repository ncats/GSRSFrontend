import { ScrollNavItemDirective } from './scroll-nav-item.directive';

describe('ScrollNavItemDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollNavItemDirective();
    expect(directive).toBeTruthy();
  });
});
