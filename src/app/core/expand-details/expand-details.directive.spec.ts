import { ExpandDetailsDirective } from './expand-details.directive';

describe('ExpandDetailsDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpandDetailsDirective();
    expect(directive).toBeTruthy();
  });
});
