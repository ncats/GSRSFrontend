import { ScrollToTargetDirective } from './scroll-to-target.directive';

describe('ScrollToTargetDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollToTargetDirective();
    expect(directive).toBeTruthy();
  });
});
