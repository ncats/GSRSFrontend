import { FacetDisplayPipe } from './facet-display.pipe';

describe('FacetDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new FacetDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
